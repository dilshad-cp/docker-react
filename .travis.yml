language: generic
sudo: required
  services:
    - docker

before_install:
  - docker build -t dilshadcp/docker-react -f Dockerfile.dev .

script:
 - docker run -e CI=true dilshad-cp/docker-react npm run test -- --coverage